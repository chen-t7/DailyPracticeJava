<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.community.mapper.DiscussPostMapper">
    <sql id="selectFields">
        id, user_id, title,content,type,status,comment_count,score,create_time
    </sql>

    <select id="selectDiscussByPage" resultType="com.blog.community.model.DiscussPost">
        select <include refid="selectFields"></include> from discuss_post
        where status!=2
        <if test="userId!=null">
            and user_id = #{userId}
        </if>
        order by type desc, create_time desc
        limit #{offset}, #{limit}
    </select>

    <select id="selectDiscussPostRows">
        select count(id) from discuss_post
        where status!=2
        <if test="userId!=null">
            and user_id = #{userId}
        </if>
    </select>
</mapper>